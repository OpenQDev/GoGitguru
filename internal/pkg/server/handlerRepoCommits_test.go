package server

import "testing"

func TestHandlerRepoCommits(t *testing.T) {
	tests := []struct {
		name string
	}{
		{
			name: "stub",
		},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {})
	}
}
